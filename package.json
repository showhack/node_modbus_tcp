{
  "name": "node_modbus_tcp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "ts-node-dev src/server.ts && src/client.ts && src/index.ts",
    "build": "npx swc src/ -d dist/",
    "start:client": "npm run build && node dist/client.js",
    "start:server": "npm run build && node dist/server.js",
    "start:ind": "npm run build && node dist/index.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@swc/cli": "^0.1.62",
    "@swc/core": "^1.3.52",
    "ts-node-dev": "^2.0.0"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jsmodbus": "^4.0.6"
  }
}
